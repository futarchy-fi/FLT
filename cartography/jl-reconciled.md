# Jacquet–Langlands — reconciliation record + adjudication (hub-lsb1u.4.3)

Provenance: the reconciling agent completed its full analysis but its document
write was destroyed by the 2026-08-14 disk-full incident, and the resumed
agent (fresh context) correctly refused to reconstruct from nothing. This
record preserves the reconciler's findings summary VERBATIM as reported at
completion time, plus the orchestrator's adjudication. The two source maps
remain canonical for node-level detail:
`cartography/jacquet-langlands.md` (pass 1, Sol) and
`cartography-b/jl.md` (pass 2, Claude+web). A full node-by-node merged
inventory is to be regenerated as the first step of this chapter's panel.

## Reconciler findings (preserved verbatim)

- Agreements (10): invocation sites, no Lean JL statement exists,
  route-minimal setting (even-degree totally real F, discriminant-1 totally
  definite D, weight 2, U1(S), only good-prime T_v traces consumed),
  mult-one as separate input, quaternionic mult-one via JL+strong-mult-one,
  Mathlib gaps, repo seeds, XL proof off critical path, consumer beads
  .5/.9/.10.
- Key divergence: pass 2's ABSORPTION vs pass 1's standalone framing —
  verified in source: `Automorphic.lean:67` defines automorphy
  quaternionically (no GL2 side), `:127` `cyclic_base_change := sorry`
  already absorbs JL-both-directions + mult-one + image classification, and
  `Assumptions/README.md` states the Galois-rep rephrasing plan (hedged
  "probably" — JL is still listed by name).
- Recommendation: Route A (absorb), bead size S–M, with two riders: a
  hidden-content ledger in each absorbing axiom (.5/.9/.10) for charter
  compliance (absorption defers, does not delete, the XL transfer proof),
  and a non-blocking N2-only eigensystem-level hedge draft so standalone JL
  can be activated cheaply.
- Secondary resolutions: pass 2 wins on finite-place local JL (identity — D
  split everywhere), norm-factoring-forms exclusion (naive bijection is
  false), U1(S,Q) Taylor–Wiles level scope, ℂ↔ℚ̄_p coefficient bridge
  (missing from pass 1); Shimizu theta = default deprecation target,
  trace-formula route kept as CBC-infra-sharing alternative; node 10
  reassigned to bead .5.
- Merged inventory: 13 nodes (7 active assumption-phase, 2 dormant Route-B,
  4 deprecation-phase XL), each with confidence tags and both-pass
  provenance mapping. [Node-level detail lost with the document; regenerate
  at panel time from the two canonical maps.]
- Panel questions (7): absorb-vs-state adjudication; ledger/ownership;
  U1(S,Q) scope; mult-one as separate bead; Shimizu vs trace formula;
  norm-factoring exclusion; coefficient-bridge preference.
- Ready-now candidates (6): quaternionic mult-one N2-only statement
  (highest value), Petersson semisimplicity lemmas, eigenvalue algebraicity,
  ledger prose, hedge JL sketch, the `sorry` at `Automorphic.lean:100`.

## Adjudication (Fable, 2026-08-14)

**Route A (absorption) is RATIFIED**, answering panel question 1 ahead of
the panel because both independent passes and the reconciler converge and
the repo's own architecture already embodies it. Binding riders:

1. **Hidden-content ledger is mandatory.** Each absorbing axiom
   (`cyclic_base_change` and the automorphic-induction and
   Galois-rep-attachment axioms, beads hub-lsb1u.5/.9/.10) must carry an
   explicit ledger comment enumerating the JL content it absorbs
   (transfer both directions, strong mult-one, image classification,
   norm-factoring exclusion). The charter's zero-axiom success criterion
   applies to the ABSORBED content: deferral is sequencing, never deletion.
2. **Hedge draft stays non-blocking**: an eigensystem-level standalone JL
   statement sketch is kept in the chapter so Route B can be activated
   without re-cartography if the absorption boundary ever fails.

Remaining panel questions 2–7 go to the chapter panel as listed.
