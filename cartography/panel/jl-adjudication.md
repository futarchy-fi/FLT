# JL chapter — panel adjudication (hub-lsb1u.4.4)

Adjudicator: Fable, 2026-08-14. Seats: literature, hypothesis/boundary,
dependency+merge-regeneration (`panel/jl-*.md`), against
`cartography/jl-reconciled.md`. The regenerated 13-node inventory in
`panel/jl-dependency.md` supersedes the placeholder in the reconciliation
record.

## Ruling: Route A STANDS, but the absorption boundary is NOT CLOSED.
No absorbing axiom may be pinned until the two holes are repaired.

**Hole H1 — automorphic induction is ORPHANED.** Named in the blueprint,
carried by no absorbing axiom in Lean or in any chapter's G-schema.
Assignment: the CBC chapter owns it (its reconciled map already scopes
AI to the quadratic-CM case as node S4); that axiom, when stated, carries
the AI content WITH ledger. Cross-chapter edge recorded.

**Hole H2 — U₁(S,Q) levels.** `IsAutomorphicOfLevel` hardcodes Q = ∅;
`cyclic_base_change` therefore never quantifies over the TW-augmented
levels that R=T patching consumes. Latent (zero consumers today), real.
BLOCKS G9/axiom pinning until either the level structure is generalized
over Q or the restriction is proven sufficient for the patching argument
(burden on whoever pins).

**Repairs (non-blocking of P1, binding on P2/axiomatization):**
1. Ledger implementation is OVERDUE: `cyclic_base_change` carries no
   hidden-content ledger comment today. The Route-A rider binds at
   axiomatization, but the comment goes in NOW (ready-now item; include
   norm-factoring exclusion per PQ6 and the coefficient bridge per PQ7).
2. Shimizu-default DEMOTED to open route question: two seats converged
   against it (original theorem is weight > 2 — needs a stronger
   citation; theta/Weil infra is MORE absent from Mathlib than
   trace-formula machinery). Decision deferred to deprecation phase,
   which is sequenced last anyway.
3. Citation re-anchors for the ledgers: weight-2 transfer (replace bare
   Shimizu 1972), ℂ↔ℚ̄_p bridge = Shimura parallel-weight integrality
   (+ Sakugawa–Sugiyama 2024); the dropped jl-b R5 hedge is restored.
4. Nonempty-instance obligation: the mod-3/Frey base case discharges via
   the Odlyzko/Fontaine `mod_three` route (bead .6) — NOT Langlands–
   Tunnell, which this campaign's route bypasses (L–T chapter is
   deferred-contingency). The seat's L–T phrasing is corrected here to
   keep cross-chapter consistency; the obligation itself is real and
   tracked on the Odlyzko chapter.
5. Misattribution fixed: the `Automorphic.lean:100` sorried instance is
   quaternion-algebra base-change content — reassigned to CBC (.5);
   the Galois-reps panel's LOW risk grade carries over unchanged.
6. Petersson semisimplicity: ready-now item retired as mostly shipped
   (`isSymmetric_toModuleEnd_T`, `Eigenform.isInternal` exist sorry-free);
   the residual is oldform/newform separation, regraded accordingly.
